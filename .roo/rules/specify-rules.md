# weborder2 Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-03-01

## Active Technologies
- TypeScript 5.x（Node.js 20 LTS；前端 TSX） + React、Material UI（@mui/material + emotion）、Fastify、zod、jsonwebtoken (001-add-auth-pages)
- 既有後端帳號資料儲存（關聯式資料庫）+ 前端 session token（httpOnly cookie 或等效既有機制） (001-add-auth-pages)

- TypeScript 5.x on Node.js 20 LTS + Fastify, zod, @node-rs/argon2, jsonwebtoken, nodemailer (001-user-registration-service)

## Project Structure

```text
src/
tests/
```

## Commands

npm test && npm run lint

## Code Style

TypeScript 5.x on Node.js 20 LTS: Follow standard conventions

## Recent Changes
- 001-add-auth-pages: Added TypeScript 5.x（Node.js 20 LTS；前端 TSX） + React、Material UI（@mui/material + emotion）、Fastify、zod、jsonwebtoken

- 001-user-registration-service: Added TypeScript 5.x on Node.js 20 LTS + Fastify, zod, @node-rs/argon2, jsonwebtoken, nodemailer

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
